<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pail.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Pail.App.Controls"
    xmlns:converters="using:Pail.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives"
    mc:Ignorable="d"
    KeyboardAcceleratorPlacementMode="Hidden"
    PointerPressed="OnPointerPressed"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.KeyboardAccelerators>
        <KeyboardAccelerator Key="Left" Modifiers="Menu" Invoked="OnPageBackKeyboardAcceleratorInvoked"/>
        <KeyboardAccelerator Key="J" Modifiers="Control" Invoked="OnDownloadsKeyboardAcceleratorInvoked"/>
    </Page.KeyboardAccelerators>

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <NavigationView
        x:Uid="MainNavigationView"
        x:Name="NavView"
        AlwaysShowHeader="False"
        BackRequested="OnBackRequested"
        IsBackButtonVisible="Visible"
        IsPaneOpen="False"
        IsSettingsVisible="True"
        PaneDisplayMode="LeftCompact"
        ItemInvoked="OnItemInvoked"
        AutomationProperties.LandmarkType="Navigation"
        AutomationProperties.LocalizedLandmarkType="Main navigation">

        <NavigationView.MenuItems>
            <NavigationViewItem x:Uid="BucketsNavItem" x:Name="BucketsNavItem" Content="Buckets" Tag="BucketListPage">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="List" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem
                x:Uid="DownloadsNavItem"
                x:Name="DownloadsNavItem"
                Content="Downloads"
                Tag="Downloads"
                ToolTipService.ToolTip="Downloads (Ctrl+J)"
                AutomationProperties.AcceleratorKey="Ctrl+J">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Download" />
                </NavigationViewItem.Icon>
                <NavigationViewItem.InfoBadge>
                    <InfoBadge
                        Value="{x:Bind DownloadViewModel.ActiveCount, Mode=OneWay}"
                        Visibility="{x:Bind DownloadViewModel.HasActiveDownloads, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                </NavigationViewItem.InfoBadge>
                <primitives:FlyoutBase.AttachedFlyout>
                    <Flyout x:Name="DownloadsFlyout" Placement="Right">
                        <Flyout.FlyoutPresenterStyle>
                            <Style TargetType="FlyoutPresenter" BasedOn="{StaticResource DefaultFlyoutPresenterStyle}">
                                <Setter Property="MinWidth" Value="560" />
                                <Setter Property="MaxWidth" Value="560" />
                                <Setter Property="MinHeight" Value="512" />
                                <Setter Property="MaxHeight" Value="512" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                            </Style>
                        </Flyout.FlyoutPresenterStyle>
                        <controls:DownloadQueueFlyout x:Name="DownloadQueueControl" ViewModel="{x:Bind DownloadViewModel, Mode=OneWay}" />
                    </Flyout>
                </primitives:FlyoutBase.AttachedFlyout>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border
                Padding="24,8,24,0"
                Visibility="{x:Bind HasSessionInfo, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                <TextBlock
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind SessionInfoText, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis"
                    ToolTipService.ToolTip="{x:Bind SessionInfoToolTip, Mode=OneWay}" />
            </Border>

            <Frame
                x:Uid="ContentFrame"
                x:Name="ContentFrame"
                Grid.Row="1"
                Navigated="OnContentFrameNavigated"
                AutomationProperties.LandmarkType="Main"
                AutomationProperties.LocalizedLandmarkType="Page content" />
        </Grid>
    </NavigationView>
</Page>
