<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pail.App.Views.BucketListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Pail.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <TextBlock Text="S3 Buckets" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <Button Content="Refresh" Command="{x:Bind ViewModel.LoadBucketsCommand}" VerticalAlignment="Center"/>
        </StackPanel>

        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Buckets}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OnBucketClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:S3BucketItem">
                    <Grid Padding="12">
                        <StackPanel>
                            <TextBlock Text="{x:Bind Name}" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind CreationDate}" FontSize="12" Foreground="Gray"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressRing Grid.Row="1" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</Page>
